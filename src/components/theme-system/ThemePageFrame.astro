---
type Props = {
  class?: string;
  pageClass?: string;
};

const { class: className = "", pageClass = "" } = Astro.props;
---

<main class:list={["theme-page-shell", className]}>
  <div class:list={["page", pageClass]}>
    <slot />
  </div>
</main>
